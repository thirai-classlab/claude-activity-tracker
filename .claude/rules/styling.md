---
paths:
  - "server/src/**/*.css"
  - "server/src/**/*.tsx"
  - "server/src/components/**/*"
  - "server/views/**/*"
  - "server/public/**/*.css"
---

# デザインルール

## カラー

（プロジェクトのカラーパレット・利用ルールを記載）

## タイポグラフィ

（見出しレベルごとの装飾ルール、ジャンプ率、フォントを記載）

## レスポンシブ

（ブレークポイントとレイアウト切り替えルールを記載）
